import styled from 'styled-components';

import NormalIcon from 'components/Icon';
import * as colors from 'utils/colors';

const TodoIcon = styled(NormalIcon)`
  fill: ${colors.darkBlue};
  width:50px;
  height: 50px;
  padding: 15px;
`;

export default TodoIcon;
